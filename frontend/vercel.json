{
  "version": 2,
  "builds": [
    { "src": "*.php", "use": "vercel-php@0.7.4" }
  ],
  "routes": [
    { "src": "/assets/(.*)", "dest": "/assets/$1" },
    { "src": "/includes/(.*)", "dest": "/includes/$1" },
    { "src": "/(.*)", "dest": "/$1" }
  ]
}
